/**
 * Replaced at build time by Vite's `define`. Declared rather than imported because it must be a
 * literal in the emitted bundle — the manifest names the same host, and the two are resolved together.
 */
declare const __WORKSPACE_ORIGIN__: string;
