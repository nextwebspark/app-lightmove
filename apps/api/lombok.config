# Root of the Lombok search — do not look further up the tree.
config.stopBubbling = true

# Tag generated constructors/getters with @lombok.Generated so JaCoCo and the like skip them.
lombok.addLombokGeneratedAnnotation = true
