-- Hiring urgency leaves the brief.
--
-- It was a three-way control on step two — 90, 60 or 30 days — and it is not a fact about the role.
-- When a mandate has to close is the project's target date, which the brief already shows on step
-- three and does not own. Dropped rather than left unread, following V38's retirement of the
-- position lock: a column nothing writes is a column the next reader has to rule out.
ALTER TABLE app_lm_position DROP COLUMN hiring_urgency;
