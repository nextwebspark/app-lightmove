package app.lightmove.api.project.constant;

/** The two competency-weighting panels; each must total exactly 100% before a position can lock. */
public enum CompetencyPanel {
    TECHNICAL,
    BEHAVIOURAL
}
