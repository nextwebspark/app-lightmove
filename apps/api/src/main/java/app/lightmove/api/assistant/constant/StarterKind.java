package app.lightmove.api.assistant.constant;

/** Why a starter prompt was offered: the firm's own sector, a sector next to it, or its size. */
public enum StarterKind {
    SECTOR,
    ADJACENT,
    SIZE
}
