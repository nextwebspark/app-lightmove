package app.lightmove.api.report.constant;

/** The table's quality badge: whether the executives someone filed are, on average, half complete. */
public enum SourcingQualityLevel {
    GOOD,
    ATTENTION
}
