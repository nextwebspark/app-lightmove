/**
 * The HTTP contract for authentication. The validation messages are what the user reads under the
 * field, so they read as instructions ("Use at least 8 characters") and mirror the frontend's Zod
 * schema.
 */
package app.lightmove.api.core.security.dto;
