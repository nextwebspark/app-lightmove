package app.lightmove.api.position.constant;

/**
 * The period the base salary band is quoted over. Load-bearing rather than cosmetic: the same pair of
 * figures means two things a factor of twelve apart, and every reader of the band needs to know which.
 */
public enum BaseSalaryMode {
    ANNUAL,
    MONTHLY
}
