/**
 * The read and its four chapter reporters. {@code ReportService} gathers once and delegates; each
 * reporter answers one chapter from the same sources, so the chapters can never disagree on a count.
 */
package app.lightmove.api.report.service;
