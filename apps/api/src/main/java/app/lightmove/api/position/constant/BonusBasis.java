package app.lightmove.api.position.constant;

/** What the target bonus figure is a proportion of — the unit that makes the number mean something. */
public enum BonusBasis {
    PERCENT_OF_BASE,
    PERCENT_OF_TOTAL_FIXED,
    MONTHS_OF_BASE
}
