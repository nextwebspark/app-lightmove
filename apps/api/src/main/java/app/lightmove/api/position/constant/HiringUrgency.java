package app.lightmove.api.position.constant;

/** How fast the mandate is expected to close — the screen labels these 90, 60 and 30 days. */
public enum HiringUrgency {
    STANDARD,
    PRIORITY,
    URGENT
}
