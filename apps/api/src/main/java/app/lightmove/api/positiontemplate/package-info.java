/**
 * <b>Position template — the briefs a new mandate is drafted from.</b> The shared library (V42), each
 * firm's copies and own templates (V58), the picker, and the JSON export/import. A firm's template
 * shadows the library's under the same {@code code}.
 *
 * <p>Never depends on {@link app.lightmove.api.position}; the vocabulary both speak lives in
 * {@code common.constant} for that reason.
 */
package app.lightmove.api.positiontemplate;
