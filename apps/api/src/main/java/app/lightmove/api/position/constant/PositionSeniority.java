package app.lightmove.api.position.constant;

/**
 * How far the seat sits from the chief executive. Named apart from {@code CandidateSeniority}, which
 * reads the same ladder from the other end: this one describes the mandate, that one a person.
 */
public enum PositionSeniority {
    C_SUITE,
    N_MINUS_1,
    N_MINUS_2,
    N_MINUS_3
}
