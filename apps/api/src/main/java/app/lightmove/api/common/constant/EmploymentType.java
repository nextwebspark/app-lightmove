package app.lightmove.api.common.constant;

/** The employment arrangement of a mandate — a closed set, mirrored by the Position screen's chips. */
public enum EmploymentType {
    FULL_TIME_PERMANENT,
    FIXED_TERM_CONTRACT,
    PART_TIME,
    TEMPORARY,
    INTERIM,
    RETAINED_ADVISORY
}
