package app.lightmove.api.project.constant;

/** Role on one project, orthogonal to the workspace role — a workspace RESEARCHER may lead a project. */
public enum ProjectRole {
    LEAD,
    MEMBER
}
