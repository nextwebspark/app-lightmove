package app.lightmove.api.candidate.constant;

/** What asked for a candidate's AI enrichment: a capture's research landing, or the drawer's button. */
public enum AiEnrichTrigger {
    CAPTURE,
    BUTTON
}
