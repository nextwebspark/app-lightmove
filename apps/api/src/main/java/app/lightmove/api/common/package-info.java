/**
 * <b>The vocabulary more than one feature is written in.</b> Seniority is the clearest case: a brief
 * asks for an N-1 and a researcher records a candidate as an N-1, so one enum answers both.
 *
 * <p>A value belongs here only once a second feature genuinely needs it; until then it stays with the
 * feature that owns it.
 */
package app.lightmove.api.common;
