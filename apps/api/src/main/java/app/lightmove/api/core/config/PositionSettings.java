package app.lightmove.api.core.config;

/** The position brief's own tunables. One branch today: the attached position description. */
public record PositionSettings(PositionDocumentSettings document) {}
