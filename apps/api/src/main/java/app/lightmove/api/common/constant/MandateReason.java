package app.lightmove.api.common.constant;

/** Why the mandate exists — shared because a role template can draft it as well as a brief record it. */
public enum MandateReason {
    NEW_ROLE,
    BACKFILL,
    SUCCESSION,
    RESTRUCTURING,
    GROWTH_EXPANSION
}
